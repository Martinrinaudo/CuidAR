# Variables de entorno para CuidAR
# Copiá este archivo a .env y completá con tus valores reales

# Connection string de PostgreSQL (Supabase)
DATABASE_URL=Host=db.xxxxx.supabase.co;Database=postgres;Username=postgres;Password=YOUR_PASSWORD;Port=5432;SSL Mode=Prefer;Trust Server Certificate=true

# Clave secreta para JWT
JWT_KEY=YOUR_SECRET_KEY_HERE_MINIMUM_32_CHARACTERS

# Orígenes permitidos para CORS (separados por comas)
ALLOWED_ORIGINS=http://localhost:4200,https://your-frontend.vercel.app

# Puerto (Railway lo configura automáticamente)
PORT=8080
